// import * as CompanyManageAPI from '@/api/companymanage'

const state = {
//   fixedCost: null,
}
const mutations = {
//   setData(state, data) {
//     state.fixedCost = data
//   },
}
const actions = {
//   async getFixedCost({ commit }, param) {
    // commit('setData', null)
    // const fixedCost = await CompanyManageAPI.getFixedCost(param)
    // console.log('固定成本数据', fixedCost.data)
    // commit('setData', fixedCost.data)
//   },
}
export default {
  namespaced: true,
  state,
  mutations,
  actions,
}

const state = {
  name: 'company',
  // 车辆状态 tooltip 二级弹窗
  popCarBaseMsg: false,
}
const actions = {}
const mutations = {
  companyOpen(state, name) {
    state[name] = true
  },
  companyClose(state, name) {
    state[name] = false
  },
}

export default {
  namespaced: true,
  state,
  actions,
  mutations,
}

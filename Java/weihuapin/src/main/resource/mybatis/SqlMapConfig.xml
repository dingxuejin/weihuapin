<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE configuration
        PUBLIC "-//mybatis.org//DTD Config 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-config.dtd">
<configuration>

    <settings>

        <!--&lt;!&ndash; 开启二级缓存  默认值为true &ndash;&gt;-->

        <setting name="cacheEnabled" value="true"/>

    </settings>
    <!-- 定义 别名 -->
    <typeAliases>
        <!-- 批量别名定义 指定包路径，自动扫描包下边的pojo，定义别名，别名默认为类名（首字母小写或大写） -->
        <package name="com.mobiusVision.pojo" />

    </typeAliases>
    <!-- 全局配置参数，需要时再设置 -->


    <!-- 和spring整合后 environments配置将废除
    <environments default="development">
        <environment id="development">-->
            <!-- 使用jdbc事务管理
            <transactionManager type="JDBC" />-->
            <!-- 数据库连接池
            <dataSource type="POOLED">
                <property name="driver" value="com.mysql.jdbc.Driver" />
                <property name="url" value="jdbc:mysql://localhost:3306/dct_v10?characterEncoding=utf-8&amp;serverTimezone=UTC" />
                <property name="username" value="root" />
                <property name="password" value="ok" />
            </dataSource>
        </environment>
    </environments>-->



    <!--加载mapper
    <mappers>
        <package name="com.mobiusVision.dao"/>
    </mappers>-->

</configuration>